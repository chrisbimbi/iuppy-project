NODE_ENV=development
PORT=3000

# Postgres rodando LOCAL (fora do container)
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=iuppy_admin
DB_PASSWORD=Iuppy#2025
DB_NAME=iuppy_dev

# Auth
JWT_ACCESS_SECRET=dev_access_secret_change_me
JWT_REFRESH_SECRET=dev_refresh_secret_change_me
JWT_ACCESS_TTL=900s
JWT_REFRESH_TTL=7d

# CORS do CMS (Vite normalmente 5173). Pode separar por vírgula se tiver mais.
CMS_ORIGIN=http://localhost:5173