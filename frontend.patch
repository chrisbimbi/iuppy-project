--- analytics/views/ContentsOverviewPage.tsx
+++ analytics/views/ContentsOverviewPage.tsx
@@ -62,7 +62,10 @@
   ack: '#0095E8',
 }
 
-const ContentsOverviewPage: React.FC = () => {
+const Contentstype SortBy = 'createdAt' | 'open' | 'unique' | 'ack' | 'reactions' | 'comments' | 'shares' | 'title'
+type SortDir = 'asc' | 'desc'
+
+OverviewPage: React.FC = () => {
   const { currentUser } = useAuth()
   const { can } = useAccess()
   const navigate = useNavigate()
@@ -79,8 +82,8 @@
     return [formatISO(from, { representation: 'date' }), formatISO(to, { representation: 'date' })]
   }, [])
 
-  const [sortBy, setSortBy] = useState<'createdAt' | 'open' | 'unique' | 'ack' | 'reactions' | 'comments' | 'shares' | 'title'>('open')
-  const [sortDir, setSortDir] = useState<'asc' | 'desc'>('desc')
+  const [sortBy, setSortBy] = useState<SortBy>('open')
+  const [sortDir, setSortDir] = useState<SortDir>('desc')
   const [page, setPage] = useState(1)
   const [pageSize, setPageSize] = useState(25)
 